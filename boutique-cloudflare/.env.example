# Configuration du Bot Telegram
# Obtenez votre token depuis @BotFather sur Telegram
BOT_TOKEN=your_bot_token_here

# Votre ID Telegram (envoyez /id au bot @userinfobot pour l'obtenir)
ADMIN_ID=your_telegram_id_here

# URL du webhook (sera automatiquement détectée sur Render/Railway/Vercel)
# Laissez vide pour utiliser l'auto-détection
WEBHOOK_URL=

# MongoDB URI (optionnel - si vide, utilise le stockage local)
# Format: mongodb+srv://username:password@cluster.mongodb.net/database
MONGODB_URI=

# Environnement (production active le keep-alive automatique)
NODE_ENV=production

# Activer le keep-alive même en développement (optionnel)
ENABLE_KEEPALIVE=false

# Port du serveur (utilisé automatiquement par les plateformes d'hébergement)
PORT=3000